import File from '../../javascript/classes/file';

describe('File', ()=> {
  
  it('uses the file API to consume JSON feeds', ()=> {
    
  });
  
  it('features basic command validation', ()=> {
    
  });
  
  it('broadcasts game-wide movement lists', ()=> {
    
  });
  
});